const SuccessButton = (name) => {
  return (
    <button className="w-full py-2 text-xs text-white bg-green-800 rounded-md sm:text-base">
      {name}
    </button>
  );
};

export default SuccessButton;
